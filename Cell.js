class Cell {

    constructor(x, y, width, height) {
        this.objects = [];
        this.position = new Vector(x, y)
        this.width = width;
        this.height = height;
    }

    show(){
        
    }

}