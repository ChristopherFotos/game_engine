

class Grid {

    constructor(scene, width, height, x, y, cellSize){
        this.scene = scene;
        this.width = width;
        this.height = height;
        this.x = x;
        this.y = y;
        this.cells = [];
        this.cellSize = cellSize;
    }

    init(){
        let position 
        while(position < this.bounds.width){
            
        }
    }

    insertObject(){

    }

    showGrid(){

    }

}